package rs.ac.uns.ftn.informatika.jpa.model;

public class Destination extends Location{

    public Destination(String address, float longitude, float latitude) {
        super(address, longitude, latitude);
    }
}
